<div class="loader">
  <p>Payment processing...</p>
</div>

<script src="https://js.stripe.com/v3/"></script>
      <script>
          const stripe = Stripe('<%= ENV['STRIPE_PUBLISHABLE_KEY'] %>');
          stripe.redirectToCheckout({
            sessionId: '<%= Basket.find(params[:basket]).checkout_session_id %>'
          });
      </script>
