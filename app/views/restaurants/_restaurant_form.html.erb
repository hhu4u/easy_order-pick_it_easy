  <%= simple_form_for(restaurant) do |f| %>
      <%= f.input :name %>
      <%= f.input :address %>
      <%= f.input :photo, as: :file %>
      <%= f.submit "Confirm", class: "btn btn-primary" %>
    <% end %>
